# STATUS - teknobu-kit

## Now
- **kit v4.6 is RELEASED** (PR #8 merged 2026-08-28, tag + zip cut, this machine updated). It carries fixes for defects that were live in v4.5 - see Done recently.
- **kit v4.7 is committed on branch `spike/fast-lane` (babfeb2), 290 tests green, NOT merged and NOT reviewed to a verdict.** `code-reviewer` was still running when it was committed, and it had been reading that worktree while the worktree changed under it - so its findings, when they land, need checking for staleness before they are acted on. security-reviewer's pass is done and every finding was acted on.
- **v4.7 contents:** `.githooks/checks` lines may carry a `[glob]` prefix and are skipped when the push touches none of those paths; `ci.yml` now generates its steps *from* that file so every line still runs somewhere; `spike/ draft/ proto/` branches report review debt instead of blocking; and `uat-writer` pushes to UAT Hub instead of writing a Markdown file the CI gate then demanded anyway.
- **v4.7 dropped a feature on review evidence:** design-only diffs were briefly going to skip `code-reviewer`. Measured, `useAuth.tsx`, `AuthProvider.tsx`, `supabaseAdmin.tsx` and a `tailwind.config.js` running `child_process.execSync` all classified as design-only. The justification was circular - the reviewer is what catches the design-lane rule being broken. File extension is the wrong axis; the content of the diff would be the right one.
- **UAT Hub is still switched off: `UAT_HUB_KEY` is not set on this machine.** Nothing pushes UAT until it is. Note the failure mode the hub's prompt documents: a key set with `setx` *after* Claude Code was running is absent from the MCP server's environment, so the hub answers "invalid or revoked api key" for a good key. Restart before suspecting it.
- **kit v4.6 on `prelive`, reviewed clear, not yet merged.** Three fixes the rollout needed - the shipped commands now run in PowerShell, `refresh --uat-project <slug>` so the rollout verb can set a repo's hub project, and a `/update` command. Then `code-reviewer` and `security-reviewer` ran on it **and on v4.5 retrospectively**, which is the pass v4.5 shipped without.
- **The retrospective review found real defects in released v4.5.** Blocking: `read()` returns `None` for an undecodable file as well as a missing one, so a UTF-16 `.mcp.json` - what PowerShell 5.1 redirection produces - took the *create* branch and destroyed every other MCP server in a committed file, with no backup, reported as `created`. Also: the kit's own secret scanner could not match a hub key and skipped every `*.example` file, so the `UAT_HUB_KEY=` line v4.5 added to every repo's `.env.example` was the most inviting unscanned place in the repo to paste the estate credential. Both fixed here, both with regression tests. **v4.5 is live with these defects until 4.6 merges.**
- The other findings fixed in 4.6: the slug was spliced unvalidated into CLAUDE.md (a committed `.teknobu.json` whose edits no reviewer sees, writing into the session's standing instructions); `splice()` doubled a file with damaged markers; `refresh --uat-project` discarded the slug in a repo with no `.teknobu.json`; `vercel` would push `UAT_HUB_KEY` to Preview; `check`/`doctor` reported a redirected `.mcp.json` as fine; `.mcp.json` did not trigger the security reviewer.
- kit v4.5 is released and this machine runs it. PR #7 merged 2026-08-28, release.yml cut v4.5 in 12s (tag + SoneloSolutionDevKit-v4.5.zip), `update` took it here: kit 4.3 -> 4.5, worklog 1.16 -> 1.17. Other machines need `update` once.
- **UAT Hub is wired but switched off: `UAT_HUB_KEY` is not set on this machine.** Until it is exported, every repo's `.mcp.json` resolves the placeholder to nothing and no push can succeed - the wiring is inert exactly as designed, but nobody gets UAT into the hub either. This is the one step between a released feature and a working one. Set it as a user environment variable; `doctor` reports presence only.
- **It shipped unreviewed.** `code-reviewer` and `security-reviewer` never ran on the kit v4.5 diff - the authoring session was instructed not to launch agents - and worklog 1.17 was merged while STATUS still said "awaiting review". Both are now in production. Recording it because that is twice in a row (see kit v4.4, parked for the same reason): the gap was disclosed each time and merged anyway, so the gate is not doing the work it was written to do.
- kit v4.5 detail: UAT Hub wiring, 173 tests green. Every repo the kit touches comes out able to push UAT to https://testing.teknobugroup.com: `.mcp.json` (merged, never replacing another server), a managed "Writing UAT" block in CLAUDE.md copied verbatim from the hub's `docs/AGENT_PROMPT.md`, `UAT_HUB_KEY` in `.env.example`, a `doctor` presence line, and a per-repo slug asked once and remembered in `.teknobu.json`. ADR-0006 records why the hub URL and checkout path are constants and the slug is not. **Reviewers were not run — the session was instructed not to launch agents — so `code-reviewer` and `security-reviewer` are outstanding before this merges.** The security-relevant surface is small and deliberate: the kit writes a credential *reference* into a committed file, and `NoLiteralKeyEverWritten` greps everything it produces with a real key in the environment.
- **VERSION is 4.5, not 4.4** — `kit-4.4` (parked, unreleased) already claims 4.4, and two artefacts under one number is worse than a gap.
- worklog v1.17 (released in v4.5, 145 of the 173 tests are its own). The dashboard now shows **agent-hours** (effort - parallel sessions add up) beside **elapsed** (wall clock - concurrent sessions count once), and the elapsed surfaces name themselves. Prompted by: the same day could be read as two different numbers with only one of them labelled. Review then found a real defect underneath - a session with no bursts counted its whole span as activity, putting 24h of Claude against 18 and 19 Aug and overstating elapsed by ~49h over 28 days. Fixed and verified against the live data.
- **kit v4.4 is PARKED on branch `kit-4.4`** (commit d881136, unmerged, not released). It fixes real damage v4.3 caused — see Done recently. 151 tests green, but it is NOT reviewed to a clear verdict: two review rounds, and a new defect was introduced in each (round 1 corrupted YAML block lists; round 2 overwrote edited files, reproducing the MediaStack incident on the release written to prevent it). Both were caught by review, neither by the author. security-reviewer's final verdict is clear and it independently confirmed the round-2 blocker fixed; code-reviewer's last word was blocked and it never re-confirmed; five edits landed after both. **Before merging: get code-reviewer to confirm the `keep` policy and `merge_frontmatter`.** Read `docs/decisions/0005` on that branch first.
- v4.5 is the released version. v4.3 is what the un-updated machines and every un-refreshed repo still run.

- kit v4.3 shipped: PR #5 merged, release.yml cut v4.3 in 13s, this machine updated. Rollout outstanding per repo (see Next 1). Every shipped agent declares its model, the two heavyweight reviewers have a reading budget, and `refresh` is the narrow verb for taking a release. ADR-0004 records why.

## Done recently
- 2026-08-28: **kit v4.5 released** - UAT Hub wiring, with worklog 1.17 riding the same PR. Two sessions' work in one tree: the doc files carrying both sets of prose were split into separate commits so neither change wore the other's commit message. The PR gate caught a real gap first - `ci-gates.yml` requires a `docs/uat/` document when code changes and neither cycle had written one; `docs/uat/prelive-2026-08-28-kit-4-5-uat-hub.md` covers kit 4.5 in 17 cases, worklog 1.17's stay in UAT_PLAN as UAT-1.17-*.
- 2026-08-28: kit v4.3 → v4.5 — UAT Hub wiring ships with the kit. Before this, `grep -c mcp repo_setup.py` returned 0 and every repo was wired by hand. Verified against a throwaway repo, not a client repo: dry run writes nothing, a second apply merges rather than duplicating, an existing `.mcp.json` with another server (and other top-level keys) survives, an unparseable one is reported not clobbered, and a grep for `uath_` across the whole tree with a real key in the environment finds nothing. The generated CLAUDE.md block was diffed against the hub's `docs/AGENT_PROMPT.md` and is byte-identical with the slug substituted.
- 2026-08-28: measured the time question before changing anything. Across 28 days: **337h effort vs 248h elapsed**, peak **5 concurrent sessions** (3.9h at 5-concurrent, 10.2h at 4, 17.3h at 3, 33.3h at 2). So concurrency is real but only ~4% of the total - parallel sessions were already adding up correctly in the report. Subagents are 5.6h of 337h (1.7%), so "agents" in the 5x8 sense means sessions, not Task runs. The much larger gap is session **span** 2,459h vs 337h active (7.3x), which is the idle cap at work - `idle_minutes` is 15 and configurable; 30 min would give 372h, 60 min 444h. Deliberately not changed: that is a judgement about what is honest to bill, and it is Phill's.
- 2026-08-28: an impact analysis caught a wrong design before it shipped - computing agent-hours by unioning each session's bursts would have run 20-40% BELOW the report, because `active_min` also carries `idle_minutes` per burst gap. The identity `active_min == sum(bursts) + idle x (bursts-1)` is now pinned by a test that builds a real transcript and runs `collect_sessions` over it.
- 2026-08-27: kit v4.2 → v4.3 — measured this machine's 28-day spend at $7,254, of which **74% is cache_read** (context re-reads: 98.1% of all token volume; output is 11%). Two causes, both configuration: the machine default was `opus[1m]`, so auto-compact never fired and sessions grew without limit; and seven of eleven agents declared no `model:`, inheriting Opus. Fixed both. Also fixed a silent bug the impact analysis surfaced: design-reviewer's carve-out in `design_files()` meant **no kit-wide frontmatter change had reached an existing repo** since that carve-out was written — it now ships from `BUILTIN_PIPELINE` like every other agent.
- 2026-08-27: machine config (not shipped) — `~/.claude/settings.json` model `opus[1m]` → `opus`; `~/.claude/worklog.json` `prices` filled for the current model line-up, so the worklog's cost column renders (it was written but dormant behind an empty `prices: {}`).
- 2026-08-25: v4.2 released and adopted here — first CI-cut release verified end to end (tag v4.2 + SoneloSolutionDevKit-v4.2.zip); `config.json` source pinned to TeknobuGroup/SoneloSolutionDevKit; Fortex nudge confirmed working, its pipeline block verified stock so the refresh is safe
- 2026-08-25: kit v4.1 → v4.2 (event-driven pipeline; dogfooding caught and fixed --update-pipeline wiping living docs; full pipeline 2 reviewers clear, findings fixed same round)
- 2026-08-24: kit v4.0 → v4.1 (new `worktree` command: new/list/clean; `/worktree` shipped to every repo via pipeline set; ADR-0002 records feature-branch-worktrees decision)
- 2026-08-24: worklog v1.15 → v1.16 (works in git worktrees; failing-test-first; 98 tests green, CI runs the suite)

## Blocked
- None

## Next
0. **Get `code-reviewer`'s v4.7 verdict, then merge `spike/fast-lane` -> `prelive` -> `main`.** Treat its findings as possibly stale: the worktree moved while it read.
0a. **Build async review before tightening anything.** Reviewers should run in a *snapshot* worktree while the session carries on, writing `.claude/state/<branch>/review.json` when they finish; the Stop gate gains a third state (in flight -> report and allow) between blocked and clear. Today is the argument: the tree was frozen twice waiting on reviewers, one reviewer **overwrote this repo's `.githooks/checks`** with its own fixture (so `pre-push` would have run three `echo`s and reported success), and two separate reviews were degraded by the tree changing mid-read. The kit already has the primitive - `worktree new` places worktrees as *siblings*, which is why the 4.7 work stayed isolated while a reviewer read a frozen `prelive`. Caveat to measure first: a fresh worktree has no `node_modules`, so a JS repo needs a persistent verify worktree or the install cost eats the gain.
0b. **A real hole, deliberately NOT fixed here: committing silences the Stop gate.** `pipeline-state.sh`'s `changed()` sees only unstaged + staged + untracked, so committed work is invisible and the gate goes quiet on any branch. It is pre-existing, it is what makes the spike lane's debt evaporate, and fixing it tightens the main gate for every repo always - the opposite direction to 0a, and more reviewer runs, which is a cost decision rather than an engineering one. Do 0a first; decide this after.
0c. **Set `UAT_HUB_KEY`, then create the hub projects.** Nothing pushes UAT until the variable is set on each machine that runs sessions - and note the failure mode the hub's prompt now documents: a key set with `setx` *after* Claude Code was already running is absent from the MCP server's environment, so the hub answers "invalid or revoked api key" for a key that is perfectly good. Restart Claude Code before suspecting the key. Then, per repo, the project must exist in UAT Hub *before* its slug is any use. **The one-key question is settled** - the hub's ADR-0004 put it again at ten projects and confirmed it, so the kit wires every repo to the same key without asking.
0b. **Merge 4.6 promptly - it carries fixes to defects that are live in 4.5.** The retrospective review is done and its findings are fixed; until 4.6 releases, any repo on 4.5 can lose its `.mcp.json` to the UTF-16 case and can commit a hub key through `.env.example` unchallenged.
0c. **One item still belongs to the hub repo, not the kit.** `.mcp.json` carries `UAT_HUB_URL`, so editing one committed string redirects where the shared key is sent. `mcp/server.mjs` still reads it from the environment and refuses to start without it, so the kit cannot drop it unilaterally; the durable fix is to pin the host inside `server.mjs` and ignore the env override. Until then the kit detects the drift two ways: the pre-commit hook refuses a `.mcp.json` naming another host, and `check`/`doctor` report it. The machine-path question is **closed** - the committed file names `${HOME:-${USERPROFILE}}/uat-hub/mcp/server.mjs` and carries no username. Note the correction that produced that form: `HOME` is **not** a Windows environment variable (absent from User and Machine scope; Git Bash sets it per process), so an earlier `${HOME}`-only version would have failed on the stated first-class platform, and the check that appeared to confirm it had been run from a Git-Bash-derived shell and could not have seen it. `claude mcp list` accepts the `:-` default form, nested included - that shows the syntax is parsed and satisfied, not that the path resolves.
1. **Roll v4.5 out, one repo at a time.** This machine is on 4.5; other machines need `update` once. Each repo also needs its UAT Hub slug passing once (`apply --uat-project <slug>`, or `refresh` then correct it) - the folder-name default is inert, not correct. Then per repo:
   `refresh` for the 4.1/4.2 repos — **Fortex** (4.2), **MediaStack** (4.1), **flexiform-shutter-vercel** (4.1), **jbm-frontend-marvel** (4.1), and this repo. Fortex and Perfect both had live sessions on 2026-08-27, so refresh those when they are quiet.
   `apply` (not `refresh`) for the pre-pipeline repos — **ForeBalls** (3.2), **Lawre** (2.0), **hearta** (1.6): they lack the git hooks, CI and `.teknobu.json` stack keys that only `apply` writes, so a refresh would leave them half-upgraded.
   Not kit repos, leave or `/repo-setup` if wanted: HeyRobin, Knecta, PuppyParent, Sonelo CRM, Verloop.
2. **Starter folder cleaned (2026-08-27).** It held ten files that shadowed kit-owned ones on a *fresh* `apply` — six stale agents (that `test-runner` had a blanket `tools: Bash` instead of the narrow allowlist), plus a **pre-v4.2 `stop-gate.sh`**, so every new repo was getting the old Stop gate. Moved to `~/.claude/sonelo/pipeline-stale-20260827/`; verified a fresh apply now lays down all 11 current agents and the current gate. The starter keeps only what the kit does not own (CLAUDE.md, README-SETUP.md, doc templates, seed docs).
3. **Kit v4.4 — cost becomes visible (bytes, and the measurement).** The parked bundle-budget plan (`~/.claude/plans/refactored-brewing-pebble.md`, budget gate in `.teknobu.json` checked by pre-push + CI, test-runner treating a recurring warning as a finding). Plus the missing half of ADR-0004: the worklog collects per-model tokens but records no per-session **context high-water mark**, so "which session sat at 400k for three hours" still cannot be answered from the report. A PostToolUse context watchdog reading `transcript_path` is the mid-session counterpart — verify the hook payload carries it before planning.
4. **Diagnose flexiform's slowness before changing it.** Reported as hours with little forward movement, all code. Three candidates and the fix differs for each: the Stop gate demanding verdicts (-> 0a), a slow pre-push suite (-> the v4.7 `[glob]` prefix), or reviewers running serially (-> 0a). Read its `.githooks/checks`, `.teknobu.json` and kit version first; there were two live sessions in that repo, so do not apply anything into it while they run.
5. Investigate pre-existing flexiform PermissionError (~/Worklog logs) and usage-table token bar min-width distortion.
